package practice.trick;

/*
 * P017 重复子树
给一颗二叉树,返回重复的 subtree。比如:
1
/ \
2 3
/ / \
4 2 4
/
4结果应该返回[ ( 2 -> 4), (4) ] 两颗树。
 */
/*

 Each subtree has a representation. check that representation exist or not.
 1. each tree is represented as string version. compare string representation.
 2. each tree has a hash value. If hash value equals, then compare tree: save memory.

 */
public class DuplicatedSubtree {
}
